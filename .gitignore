/target/

# Wallets. Encrypted, but a keystore in a public repo is a permanent offline
# brute-force target, so none of these ever belong in git.
*.keystore
wallets.json
nock-wallet.json
*wallet*.json
.env

# npm package and GitHub Release build output.
/dist/
/node_modules/
/*.tgz
